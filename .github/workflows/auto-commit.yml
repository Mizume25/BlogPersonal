name: Auto Sync and Build

on:
  push:
    branches: [ main ]
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  auto-sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write  
    
    steps:
    - name: Checkout código
      uses: actions/checkout@v4
        
    - name: Configurar Git  
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        git remote set-url origin https://github.com/Mizume25/BlogPersonal.git
        
    - name: Auto Commit cambios
      run: |
        git add .
        git diff --staged --quiet || git commit -m "🤖 Auto-sync: $(date +'%Y-%m-%d %H:%M:%S')"
        git push